const path = require('path')

module.exports = {
    'config' : path.resolve('./src/config', 'config.json'), //cho biết how to connect to db
    'migrations-path' : path.resolve('./src', 'migrations'), //tạo bảng trong db
    'model-path' : path.resolve('./src', 'model'),
    'seeders-path' : path.resolve('./src', 'seeders')
}